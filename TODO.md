# TODO
- [x] Implement access control: only user who initiates bot via /start in private chat can receive private command messages.
- [x] Persist allowed user id in `allowed_users.json`.
- [x] Block responses in non-private chats (or redirect with a message).
- [x] Refactor duplicate handler function names in `main.py` for correctness/readability.
- [ ] Sanity-check by running the bot (optional).


